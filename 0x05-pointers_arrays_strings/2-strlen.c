#include "main.h"

/**
 * strlen - Funtion to print the length of a string.
 * @s: length of string to be printed
 * Return: 0
 */
int _strlen(char *s)
{
	int g;
	g = 0;

	while (*s != '\0')
	{

	s++;
	g++;
	}

	return (0);
}
