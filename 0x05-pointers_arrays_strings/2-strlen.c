#include "main.h"

/**
 * _strelen - function to print the lenght of a string
 * @s: string to print
 * Return: int length of string
 */

void _strlen(char *s)
{
	int length = 0;

	while (s[length])
		length++
	return (length);
}
