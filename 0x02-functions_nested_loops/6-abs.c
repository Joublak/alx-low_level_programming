#include "main.h"

/**
 * _abs - return value of an interger
 * @c: the number to be checked
 * Return: 1 or 0
 */
int _abs(int n)
{
	if (n < 0)
	{
		n = n * -1;
		return (n);
	}
	else
	{
		return (n);
	}
}
